# Fixture provenance

slidewright-design-profile-source.pptx is an original synthetic fixture authored and saved by Microsoft PowerPoint for Windows through create-fixture.ps1. It contains no third-party template, media, personal data, embedded font, or proprietary brand asset.

The creation script adds a native editable grouped logo, master chrome, two named layouts, source-authored placeholders, footer and slide numbers, four guides, four exact rim/limiter pairs, and one deliberately unequal paired section rule. The adjacent SHA-bound asymmetry manifest declares why that pair must be preserved. It then replaces workstation Office author fields with the neutral value Slidewright contributors.

The committed PPTX is the canonical licensed test input. The script documents and reproduces its construction. The fixture is licensed under the adjacent MIT license.
