# Slidewright binary goal register

Status is binary: `1` means the stated evidence exists and was checked; `0` means it does not. Progress notes never change the binary definition.

Current credited status: **34/36 product goals**, **19/22 complaint-derived goals**, **53/58 total**. G09 and G22 remain uncredited product goals.

| ID | Goal | Status | Required evidence |
| --- | --- | ---: | --- |
| G01 | Valid repo-local Codex plugin and skill | 1 | Plugin validator and skill validator both pass |
| G02 | Deterministic layout compiler | 1 | Unit tests prove stable output from the same input |
| G03 | Symmetric margins and padding | 1 | Linter fixtures pass valid cases and reject asymmetric cases |
| G04 | Conventional integer font sizing | 1 | Autosizer tests plus exported OOXML font-size audit |
| G05 | Native editable text and shapes | 1 | OOXML audit finds native text nodes and no text-as-image fallback |
| G06 | Mixed bold and regular formatting | 1 | Exported PPTX contains both bold and regular runs in one text object |
| G07 | No overflow or clipping in demo | 1 | Plan lint, renderer layout export, slide test, and full-size visual inspection |
| G08 | Runnable judge path | 1 | Fresh-clone setup and demo commands succeed from documented instructions |
| G09 | Build Week submission package | 0 | Public/private judge repo access, <3 minute public demo, description, screenshots, testing instructions, and `/feedback` session ID |
| G10 | Template-preserving edit path | 1 | Golden-file test on a user-provided or licensed template with explicit deviation log |
| G11 | Owned controlled exporter-conformance benchmark | 1 | Invite, brochure, and website manifest drives six browser/PPT reference pairs |
| G12 | Object-level formatting fidelity | 1 | Final OOXML audit matches text, typeface, integer size, emphasis, color, geometry, rotation, line style, alignment, line spacing, fit mode, and insets for every named object |
| G13 | Native group integrity | 1 | Six expected `p:grpSp` groups exist, group locks are removed, and no raster fallback exists |
| G14 | Published visual-fidelity bar | 1 | Every fixture passes global, foreground, and background-normalized image gates plus exact object geometry |
| G15 | Horizontal and vertical reflow proof | 1 | Every design family has independent horizontal and vertical/mobile composition |
| G16 | Editable horizontal and vertical graphics | 1 | Native timeline, horizontal bars, and vertical bars pass structural audit |
| G17 | Copy-mutation resilience | 1 | -25% and +25% copy fixtures relayout without clipping or sub-minimum type |
| G18 | One-command evidence bundle | 1 | `npm run fidelity` emits references, PPTX, renders, audit, comparisons, and scorecard |
| G19 | Real PowerPoint ungroup/regroup round trip | 1 | PowerPoint opens, ungroups, regroups, saves, reopens, and preserves member count plus native text |
| G20 | Missing-font and dense-content repair | 1 | Fixtures fail or relayout with actionable diagnostics and never silently emit tiny text |
| G21 | Independent image-to-PowerPoint ingestion | 1 | A design image not generated from the output manifest is parsed, reconstructed, and scored without shared geometry input |
| G22 | Existing-presentation design-system extraction and reuse | 0 | A licensed source deck is ingested into a machine-readable design profile covering fonts, palette, masters/layouts, slide size, logos, orientation/divider/limiting lines, spacing, and recurring chrome; a new native-editable deck reuses the profile and passes structural, visual, and PowerPoint round-trip audits |
| G23 | Exact rim and limiting-line symmetry | 1 | Positive fixtures preserve explicitly asymmetric source rules; otherwise left/right and top/bottom rim or limiting lines have exactly equal widths, paired offsets, colors, and styles, with negative OOXML and rendered-geometry controls rejecting drift |
| G24 | Absolute text-box non-overlap | 1 | Plan, realized-layout, OOXML, and rendered negative fixtures prove that any visible text-box intersection fails, including explicitly allowed generic overlaps |
| G25 | Headline safe-width utilization | 1 | Headlines extend to the full safe width unless a declared center or two-thirds split reserves the adjacent region; shortened-title negative fixtures fail |
| G26 | Text-backed region auto-growth | 1 | Multi-line titles and callouts deterministically grow their background/container with symmetric padding or relayout; fixed-height underflow fixtures fail |
| G27 | Declared topic and chapter coverage | 1 | A coverage manifest maps every declared topic to an explicit divider and substantive slide; omitted, merged-away, or unsequenced topics fail |
| G28 | Inherited bullet and empty-paragraph hygiene | 1 | Empty paragraphs cannot emit blank bullets, consume fit budget, or cause collisions; inherited-template positive and negative fixtures pass/fail as expected |
| G29 | Universal symmetric text and table insets | 1 | Schema-level tokens, exact negative mutants, rendered text-style checks, and plan-bound native table OOXML audit prove bounded equal insets on all four sides |
| G30 | Universal text-backing containment | 1 | Strict zero-spill plan checks, a one-pixel escape mutant, realized-layout binding, and exact exported object geometry prove every declared text/backing pair remains contained |
| G31 | Repeated component hierarchy consistency | 1 | Deck-wide family/slot/variant signatures keep equivalent heading/body styles stable while allowing declared archetype variants; one-instance drift fails deterministically |
| G32 | Semantic icon-label binding | 1 | Native editable icon glyphs bind to an exact label and ontology concept, unrelated choices fail, and exported semantic metadata matches all four tested icons |
| G33 | Constrained headline editorial budgets | 1 | Archetype-specific line and one-step auto-size budgets reject aggressive shrinking while six unrelated archetypes pass rendered review |
| G34 | Native paragraph-spacing rhythm | 1 | Paragraph boundaries and 0/6/12pt spacing survive rendered-layout and OOXML audits; arbitrary and stacked spacing mutants fail |
| G35 | Universal logical master and page archetypes | 1 | A versioned design system binds every slide to one generated logical master, declared archetype, page role, typography roles, spacing tokens, and zero unresolved exceptions without falsely claiming a native PowerPoint master |
| G36 | Optional executive-review overlay | 1 | The guarded request path can publish a separate native-editable yellow partner-review copy with shape-bound findings while the canonical deck remains byte-identical to review-off mode and contains zero hidden review objects |

## Complaint-derived goal register

These goals come from the public complaint inventory in `docs/COMPLAINT_LEDGER.md`. Status remains binary and deliberately conservative.

| ID | Goal | Status | Required evidence |
| --- | --- | ---: | --- |
| C01 | Capability preflight and actionable recovery | 1 | One command detects missing skill, runtime, renderer, PowerPoint, and fonts and returns exact supported remediation |
| C02 | Discoverable self-contained install across Codex surfaces | 0 | Fresh Desktop, CLI, and VS Code checks find or install Slidewright without marketplace UI assumptions |
| C03 | Reliable clean-host runtime bootstrap | 1 | Windows, macOS, Linux, and WSL clean hosts resolve a supported runtime or fail once with a tested fallback; no pinned-asset 404 |
| C04 | Repair-free PowerPoint output | 1 | 20+ release fixtures pass ZIP, relationships/content-types, Open XML validation, and real PowerPoint open/save without repair or removed content |
| C05 | Verified delivery with no dead links | 1 | Delivery command verifies existence, nonzero size, ZIP integrity, canonical path, and link target before reporting success |
| C06 | Preview-independent handoff | 1 | Each delivery includes PPTX, per-slide PNGs, montage/PDF, manifest, and external-open instructions verified from output and workspace paths |
| C07 | Native editable visible text | 1 | Controlled six-slide audit finds every expected string in native named text objects with zero pictures |
| C08 | Complete semantic object coverage | 1 | Fresh hardened real-PowerPoint scorecard proves the complete frozen semantic surface, exact owned-process cleanup, immutable implementation/input closure, and source/round-trip full-size review |
| C09 | No text-as-image or full-slide raster fallback | 1 | Controlled benchmark has zero `p:pic` objects and 129 native editable elements |
| C10 | Template, master, layout, and brand preservation | 1 | Licensed golden templates retain masters, layouts, placeholders, theme relationships, palette, spacing, and chrome with explicit deviation report |
| C11 | Font and advanced-format integrity | 1 | Missing/substituted fonts fail visibly; licensed embedding and complex template/font fixtures pass PowerPoint round trips |
| C12 | Prompt-robust workflow | 1 | Minimal, verbose, conflicting, and adversarial prompt matrix cannot bypass compile/lint/render/audit or degrade below quality gates |
| C13 | Professional visual quality | 0 | Blind expert review and five target users meet defined first-open acceptance and cleanup-time thresholds across 20+ independent designs |
| C14 | Automated geometric and readability defects | 1 | Overlap, overflow, clipping, contrast, alignment, wrapping, crowded layout, and chart-readability checks have positive and negative fixtures |
| C15 | Dense-content and copy-mutation resilience | 1 | -25%, +25%, translation expansion, and dense-copy fixtures relayout without clipping or sub-minimum type |
| C16 | Stable fine-grained iteration | 1 | Named-object edits for text, bold, color, position, chart, and layout preserve unrelated object hashes and pass rerender comparison |
| C17 | Structural existing-PPTX ingestion | 1 | Import retains slide/master hierarchy, text runs, tables, diagrams, charts, notes, and semantic reading order on licensed decks |
| C18 | Native readable charts, tables, and diagrams | 1 | Horizontal/vertical native charts, table cells, grouped diagram nodes, and attached connectors pass real-PowerPoint mutation/reopen, readability, OOXML, destructive-control, and 24-slide review gates |
| C19 | Cross-suite interoperability | 0 | Published matrix proves open/import behavior in PowerPoint Windows/macOS, Google Slides, Keynote, LibreOffice, and Canva |
| C20 | Transparent dependencies and platform behavior | 1 | No silent install or renderer switch; lockfile, runtime version, renderer, font requirements, and supported matrix are emitted per build |
| C21 | Optional direct-PowerPoint editing fallback | 1 | Windows adapter can open, select, edit, group/ungroup, save, and verify while generation remains functional without computer-use tooling |
| C22 | Public reproducible quality evidence | 1 | Committed public benchmark, exact commands, negative controls, versioned scorecards, CI artifacts, and a fresh-machine replication report |

## Evidence register

Append dated evidence here. Do not replace the goal definitions with looser substitutes.

- 2026-07-19: C10 public evidence was refreshed after PowerPoint became available, without weakening its semantic gate. Two reproducible initial runs showed that current PowerPoint 16.0 adds only `dirty="0"` to `a:rPr` and `a:endParaRPr` nodes in the Automizer table fixture while preserving exact live native state. The auditor now normalizes only that explicit clean-cache value on those two DrawingML elements; a direct control proves `dirty="1"` remains a semantic failure. On clean exact commit `e3085df9ce2b1382c2811be795331a1331eacd39`, the full four-family, 39-slide PowerPoint template matrix then passed in 1,067.3 seconds at scorecard `f96ffb49e75a8982bd0ff08ae1d5f5183efec98a4ced7c8fb9bd064a6fe5924a`. All 195 new review renders were path-and-SHA identical to the prior all-pass inspected set, so the review was carried forward through exact byte identity at review hash `ae4b8c5e06175d0dc2c81f624717cd0decdbec9cbd1954fea42c18b405dea9c5`. Machine verification and compact public-evidence verification independently pass with four fixtures, 39 slides, 542 artifact receipts, 195 reviewed artifacts, and 48 implementation files. The final repository suite passes 308/308; plugin and skill validation pass. This restores C10 freshness but does not change the binary goal count.

- 2026-07-19: G06/G31 rich-text evidence was strengthened for the supplied wrong/correct `Label — explanation` examples without changing the binary goal count. New deck-wide linter rule `SW030` recognizes repeated bulleted and non-bulleted dash/colon patterns, requires a uniform peer explanation style, preserves label-only nuances such as bold italics, and rejects alternating whole-paragraph emphasis, a fully leaked explanation, and leakage into only the first explanation word. Named edits now synchronize flat and paragraph run models and fail closed on post-edit lint. Focused creation, bullet, colon, German translation/adaptive relayout, named-edit, and destructive-control tests pass 37/37. `npm run emphasis-pattern` renders one native editable PPTX with four mixed-emphasis paragraphs, eight explicit label/body runs, 11 native text nodes, zero pictures, approved integer type only, exact plan-bound OOXML run matches, and three isolated `SW030` controls; its full-size 1280x720 render passed visual inspection. Demo compile/lint/render/audit, the seven-slide universal-design gate, preflight, plugin validation, and skill validation pass; all 11 regenerated demo/emphasis/universal slides were inspected individually at full size. The repository suite passes 306/307; the sole failure remains the pre-existing stale C10 compact public-evidence closure for `package-lock.json`, so no new release-green or C10-refresh claim is made.

- 2026-07-19: Independent release review found and closed the first-feedback overclaims before publication. The source profiler follows actual `p:sldIdLst` display order, rejects invalid relationships, classifies topology from geometry/object graphs rather than fixture phrases, and recovers positive semantic table-column counts instead of permitting a zero-item wildcard. Reference grounding resolves both `srgbClr` and standard Office `sysClr` theme tokens; applies source-bound colors and the available source minor font without fallback or silent substitution; records the unavailable source display font plus 4 guides, 1 master, 8 used layouts, 2 logo records, and 403 chrome records; accepts only exact native adapters; strictly parses the large profile; and recomputes tokens, structure, inheritance, plan-bound substantive roles, item topology, adapter evidence, reference-derived-only diversity, mapping aggregates, and validity. The final 14-slide v29 candidate maps 100% of substantive slides with zero generic fallbacks across nine source concepts and seven observable native variants: banded matrix, centered color field, dual rail, four-callout quadrant, numbered three-column grid, split highlight, and triangular cycle. E6 production advice is domain-neutral and content-derived, repeated-source provenance is target-specific, and 14/14 changed review renders pass full-size inspection; all clean renders remain byte-identical to the previously inspected v27. Focused tests pass 38/38. The full suite passes 299/300, with only the expected stale C10 public-evidence implementation hash for the changed `package-lock.json`; demo compile/lint/render/OOXML audit and independent v29 request verification pass. Final isolated PowerPoint edit/save/close/reopen/render and C10 refresh remain pending because an unrelated user-owned PowerPoint session is active; no Office process was attached to or terminated. G22 is returned to `0`: master/layout relationships, guides, logos, limiting lines, and recurring chrome are currently auditable provenance but are not yet generally applied to a new deck, so the full prior-presentation inheritance claim is not proven. G09/G22 and C02/C13/C19 correctly remain `0`.

- 2026-07-18: The first public-main CI run after C11/C17 integration, GitHub Actions `29646825828`, correctly failed both portable core jobs because C17's contract had recorded a raw Windows-CRLF hash for two text license files while clean Git checkouts normalize them to LF. The semantic import tests and controls passed; only the license-receipt assertion failed identically on Linux and Windows. Commit `af087bca6f1d1b0c7c5eb86662797ad314dd7b4c` makes the contract explicitly `utf8-lf`, normalizes CRLF and lone CR before license hashing, and adds a direct LF/CRLF invariance test. The focused C17 suite passed 4/4, the full suite passed 267/267, `npm run evidence:ci` passed locally with 15/15 required destructive-control tests and zero demo warnings, and clean C17 release evidence passed four fixtures and eight controls at scorecard `8d12355ce10fa36319c4db93252c29ba4290f3108148f42b6019924c9d30bcc4`.

- 2026-07-18: The consolidated `npm run release:check` passed on clean exact commit `66e394bb043b44b2bb2547842d98c89b66b29197` in 5,794.8 seconds. It completed preflight with zero required failures; 266/266 tests; demo compile, zero-warning lint, native render and OOXML audit; universal design, fidelity, copy mutation/repair/resilience, independent image ingestion, template preservation, defects, direct-PowerPoint adapter, iteration, design profile, feedback and prompt contracts, semantic surface/mutation, and the new C11/C17 gates. The final C11 scorecard file hash was `b35b197e38a8a9fb6ee82f4f779296b03a0262d6f1c01528e9b9073a892c4667`; C17 published clean release scorecard `5250302ba3a6a716feec531880bae8d0586d048130a81fe89b788774a1d9d63c`. C04 freshly regenerated every upstream producer, then passed 26/26 byte-unique fixtures and 13 destructive controls at scorecard `528ffdb637b59c3c64c86963a2233e316299263a42672687bc7efc1f0a801640` with evidence-tree hash `c3f0f2c632131eda29a7817631c4df7226347302623c93d29566021bb78a47ef`. The standalone verifier passed, public replicated evidence passed, the worktree remained clean, and no PowerPoint process remained.

- 2026-07-18: C10's public evidence was regenerated after the C11/C17 release wiring changed `package.json`, rather than bypassing implementation-closure drift. On clean exact commit `a4a583a5e1eb48ddd1912da8ff57682da407978b`, the full four-template, 39-slide PowerPoint matrix passed again with compact scorecard `bf515b935ddd3e88b7b1aa318ee31d0eaa449b722d8f80363c34b7135b7e07c7`, implementation closure `ffa2891193d6c67c880dadd30d15a0fe62c62dd85efecae36d50609ec2c719a0`, 542 artifact receipts, and review `c2fd83fbb9810cfab9c34cccb71e7505d3018da37b3946d7b9fa731ac3dd001f`. All 195 new review artifacts were exact path-and-SHA matches to the individually inspected prior public review, so the pass decisions were carried forward through an explicit 195/195 byte-identity receipt with zero mismatches. `npm run template:matrix:verify` and `npm run template:matrix:verify:published` independently passed the refreshed pointer.

- 2026-07-18: C11 advanced to `1` on clean exact commit `28e73560c3ab28e2e6fd7f226ae6d46433895521`. Scorecard `d292a07fd048836ea1e5c60a44b3ea7a46bb6eeb63e9cbf0066c25dee3b1bfcb` and implementation closure `5b53b6052983fdf726122b8ece3f379b26d88d53f7e6d11e112daf9ee3410ab1` prove one OFL-licensed collision-resistant four-style family with `fsType=0`, four distinct embedded font payloads, native regular/bold/italic/bold-italic runs, a real master/layout, an editable native group, and a native table. Two isolated Microsoft PowerPoint save/reopen cycles retained the exact visible style fingerprint. All six 1600x900 source/round-trip renders collapse to two byte-identical slide hashes; both unique slides were inspected individually at original size with no clipping, overlap, unintended wrap, or style loss. The missing-family PowerPoint control and Slidewright preflight both failed visibly without substitution, and removed-payload, truncated-payload, and visible-substitution destructive controls were independently rejected. `npm run font-integrity` and `npm run font-integrity:verify` passed on the exact commit.

- 2026-07-18: C17 advanced to `1` on clean exact commit `28e73560c3ab28e2e6fd7f226ae6d46433895521`. Content-addressed release scorecard `f5a2d792e8a1006033e09d7be996eb5f3bd6fe99f3afc3c4a13d3d4458dbd7e7` with implementation hash `97a58c1ef4904bda09b369cc97d9d32a7bf7b22112a0f1d6f6af7de2d79f759c` independently verifies four licensed PPTX fixtures and all seven named surfaces: slide/layout/master/theme hierarchy, native text runs, tables, charts, diagrams, notes, and recursive semantic reading order. Three fixtures were rewritten into byte-distinct deterministic containers and the fourth remained byte-identical because its canonical source already matched; all retained exact part inventories and bytes. Eight destructive controls each removed or altered a required surface and were rejected. `npm run structural-ingestion` and `npm run structural-ingestion:verify` passed with `releaseEvidence: true`. This credits lossless structural preservation, not arbitrary editing or semantic reconstruction of imported objects.

- 2026-07-18: C02 remains `0`, but the public-main standalone skill now has a real client-originated Codex CLI proof. A fresh clean workspace installed the skill from public GitHub, passed the skill validator, bootstrapped the bundled artifact runtime, passed required preflight capabilities, and let Codex CLI 0.118.0 discover `$slidewright`, read its complaint contract, and return the exact first workflow step without being given a filesystem path. The evidence is bound to public commit `89750e2c8197041a756177859b5294b00b426555` and result hash `66c3080ed992d38b77f2c6a2ea86109f354e4c191228ee900828c21ca0b7cc9b`. Actual Codex Desktop and VS Code client-originated proofs are still missing, so the binary definition is deliberately unmet.

- 2026-07-18: C19 remains `0`. A frozen six-suite contract, schema, artifact importer, and independent verifier now require the same source deck, exact clean commit, named application/version, automation trace, native sentinel edits, re-exported PPTX package, semantic inventory, and full-slide renders for PowerPoint Windows/macOS, Google Slides, Keynote, LibreOffice, and Canva. Forty-eight destructive controls pass, eight per suite. The status runner currently detects PowerPoint on Windows but correctly reports `evidence: false`; the other required application/browser hosts are also unproven. Capability detection or a successful open can never advance C19.

- 2026-07-18: C10 advanced to `1` on exact source commit `8429ed065f23bdba43f36a571020f21b781b6ae7`. Compact public scorecard `8fcd553235e1a20156abe642794288546031b3d42e216d58a876c3a0558284be` and bound review `56d208968faac1d1ef1b404c228730f5234c5c9f32be5e8567a887ede577d0c1` independently verify four licensed template families, 39 slides, 542 exact artifact receipts, 48 implementation files, and 195/195 full-size reviewed source/edited/PowerPoint-roundtrip/repeat/visible-negative renders. The matrix retains exact masters, layouts, placeholders, theme relationships, palette, spacing, chrome, notes and native complex objects, uses source-object identities for every ordinary edit, passes two real-PowerPoint serialization cycles, and rejects chart, workbook, table, hyperlink, media, native-object, source-identity, render-inventory, implementation-closure, and visible-geometry destructive controls. `npm run template:matrix:verify:published`, 253/253 repository tests, plugin validation, skill validation, 26/26 repair-free fixtures, and 13 repair-free controls all passed. Public Core CI push run `29642280222` passed every job on the same source commit. Its GitHub-authenticated Linux artifact `8428977418` (`sha256:c2b67de62b9b56447c0f2ef7005a97e9c395e264d17ca357f8c82b9964918342`), Windows artifact `8428983109` (`sha256:0e6e83cc8a0959c4d61a24d8e44ae44b049d792ac48988e1e073f0b4bacf9c32`), and aggregate artifact `8428985242` (`sha256:3582440362f7a3cf23c5ba4b5a97a6970c2f080444073f0c996d459d227c4757`) were downloaded and imported. Default `npm run evidence:verify` then passed in replicated release mode with manifest `f348e6b6e273146579e1cd6d30f3266437712f755c78e7e15941839b6df6e3dc`, aggregate `b3082b4d740d649b7faefa47ed53235c1a006e00e65da8668d03acecc3e91215`, and portable result `bd13f658eefa06b9d8845bf72222d0720565371a6c4ae4ad841eb69bb012ec1c`.

- 2026-07-17: C10 remains `0`. Independent review invalidated the earlier candidate because the PowerPoint round trip had only shallow native chart/table checks, ordinary edits did not require complete source-object identity, render comparators had vacuous inventory paths, negative-control summaries and the implementation closure were verifier-trusted, and published G22/G23 evidence had drifted. The branch now has complete source identity binding, exact render inventories and dimensions, a static edited-package-versus-PowerPoint semantic audit, chart/workbook/table/hyperlink/media/native-object destructive controls, reproducible hash-pinned sanitizers, and a verifier-derived closure. Credit stays at zero until a fresh matrix, complete hash-bound full-size review, public-evidence refresh, and all release gates pass on the final implementation.

- 2026-07-14: Repository initialized; plugin and skill scaffolds created.
- 2026-07-14: Plugin validator and skill validator passed.
- 2026-07-14: Node test suite passed 8/8, including determinism, autosizing, rich text, symmetric geometry, and negative diagnostics.
- 2026-07-14: Demo compiled to three slides with zero lint warnings and zero lint errors.
- 2026-07-14: `slides_test.py` reported no overflow; all three LibreOffice-rendered slides were inspected at full size with no clipping, wrapping, or unintended overlap.
- 2026-07-14: OOXML audit passed with 18 native text nodes, 10 bold runs, 8 regular runs, 2 mixed-emphasis paragraphs, no fractional font sizes, and no sizes outside the approved scale.
- 2026-07-14: A clean temporary copy with no `.git`, `node_modules`, or `outputs` completed `npm run demo` end to end in 18.7 seconds.
- 2026-07-15: Six owned browser references and six native PowerPoint reconstructions completed the one-command fidelity benchmark.
- 2026-07-15: Forensic audit passed 129/129 named objects, six native groups, zero pictures, exact text/run formatting, geometry, rotation, lines, alignment, line spacing, fit mode, and insets.
- 2026-07-15: Visual comparison passed all six slides: 0.97528 global average, 0.91844 foreground average, and 0.72427 background-normalized average. The blank-slide negative control was rejected for all six. Every pair was inspected at full size; two review findings were fixed and rerun.
- 2026-07-15: `slides_test.py` passed with no overflow on the grouped benchmark deck.
- 2026-07-15: Microsoft PowerPoint ungroup/regroup/save/reopen round trip preserved all 16 slide-1 children and eight native text items.
- 2026-07-15: Final regression passed 11/11 Node tests, demo compile/lint/render/audit, benchmark overflow test, skill validation, and plugin validation; all nine rendered demo and benchmark slides were reviewed at full size.
- 2026-07-15: Public complaint sweep deduplicated direct Codex/OpenAI issue reports, Reddit complaints, official limitations, and adjacent editable-PPTX workflow failures into C01-C22. At sweep time, only C07 and C09 had the named proof.
- 2026-07-15: Capability preflight passed the real runtime and produced explicit availability plus remediation for the skill, Node, Python, artifact runtime, renderer, fonts, PowerPoint, and LibreOffice. Negative unit fixtures independently removed every required capability and confirmed a blocking result with actionable remediation; optional PowerPoint absence remained visible without disabling generation.
- 2026-07-15: Delivery verification passed the six-slide benchmark at its canonical path with 32,617 bytes, SHA-256 `263ec29cda9e91bb79e3735fe30e45b0fed3f0f11c4411613e6f244fb68e8214`, six slides, all required PPTX package parts, valid ZIP integrity, and six preview paths. Negative unit fixtures rejected missing, empty, corrupt, and wrong-extension artifacts. The one-command fidelity pipeline now runs both preflight and delivery verification.
- 2026-07-15: The same delivery emitted six matching full-slide PNGs, a montage, a machine-readable manifest, and host-specific external-open instructions. Bundle-required verification rejects missing previews, montage, or instructions.
- 2026-07-15: Dependency transparency proof added a portable lockfile, explicit Codex runtime selection, artifact-tool and renderer versions, required-font reporting, and a conservative platform matrix. A fresh temporary checkout completed clean `npm ci`, 17/17 tests, demo compile/lint with zero warnings, skill validation, and plugin validation without inheriting workspace `node_modules`.
- 2026-07-15: Whole-word -25% and +25% copy fixtures both compiled and linted with zero warnings, rendered as three editable slides each, passed `slides_test.py`, preserved approved whole-point type and mixed emphasis, passed OOXML audit and bundle verification, and were inspected individually at full size with no clipping, unintended overlap, or sub-minimum body type.
- 2026-07-15: Consolidated `npm run release:check` passed preflight, 20/20 unit tests, demo compile/lint/render/audit, the full six-slide fidelity benchmark, real PowerPoint group round trip, verified delivery bundle, and both mutation builds in 167 seconds. All 15 current demo, fidelity, and mutation renders were then inspected individually at full size.
- 2026-07-15: Missing-font audit found and removed a real silent-substitution risk in the demo, which now explicitly uses installed Arial. The renderer itself blocks unresolved fonts and returns an exact install-or-explicit-fallback remedy; the negative control applied no substitution and emitted no PPTX.
- 2026-07-15: A 4x dense-copy fixture produced ten actionable `SW004` fit failures. Every text object remained at or above its configured minimum (12pt eyebrow, 16pt body/callout, 28pt title), the renderer refused export, and no invalid or tiny-text PPTX was created.
- 2026-07-15: Updated `npm run release:check` passed 24/24 tests, font-audited demo render/audit, six-slide fidelity plus PowerPoint round trip, both mutation builds, and both repair negative controls in 168 seconds. All 15 regenerated slides were inspected individually at full size.
- 2026-07-15: Independent opaque-image ingestion passed on the first pixel-scored run after schema/exporter corrections and without changing precommitted thresholds. A fresh no-context parser viewed only metadata-free SHA-256 fixture `7a688db716046c64928d4ee197cd9e211360cd7b62f4c5db5a885fd508a85bb8.png`; the quarantined HTML source had already been deleted. The frozen observation produced 13 native text objects and 10 native shapes with zero pictures/media, no embedded source raster, no grouping locks, no overflow, and verified delivery. Pixel scoring passed at 0.95102 global, 0.93510 foreground, and 0.89922 background-normalized similarity; the blank control failed. Source, reconstruction, overlay, and difference image were inspected at full size and visible font-metric/placement deviations were retained as disclosed limitations.
- 2026-07-15: Standalone skill validation in a fresh workspace with no inherited `node_modules` completed the skill-local runtime bootstrap, preflight, compile, font audit, lint, native render, and OOXML audit. The generated one-slide PPTX contained five native text nodes, zero pictures, approved whole-point type, and mixed bold/regular runs. Local Codex CLI 0.118.0 lacks the current documented `codex plugin` command, so CLI installation remains uncredited under C02 while desktop-marketplace and direct-skill distribution are packaged.
- 2026-07-15: Consolidated `npm run release:check` passed preflight, 28/28 tests, three-slide demo compile/font/lint/render/audit, six-slide controlled fidelity and real PowerPoint ungroup/regroup round trip, both copy-mutation builds, missing-font and dense-copy negative controls, independent image ingestion, and verified delivery in 212.6 seconds. All 16 regenerated demo, fidelity, mutation, and ingestion slides were then opened and inspected individually at full size.
- 2026-07-15: Independent release review found and blocked three overclaims. The standalone loader was changed to resolve `@oai/artifact-tool` from the bootstrapped target workspace; a copied skill and sibling workspace outside the repository then completed bootstrap, preflight, compile, render, and OOXML audit with zero pictures. Parser access evidence was converted from machine-specific absolute paths to checkout-relative paths and the complete ingestion run passed from the portable verifier. Finally, edge-v1 added a 0.70 content-sensitive gate: the reconstruction passed at 0.79492 edge F1, while an all-text-erased control that still scored 0.97223/0.96039/0.94285 on the three flat-color metrics failed at 0.54784 edge F1.
- 2026-07-15: The MIT-licensed PowerPoint-authored golden-template path edited only the two named native placeholders on slide 1. Forensic comparison retained the same 39-part inventory and 36 relationship tuples, with 38 byte-identical parts and only `ppt/slides/slide1.xml` changed; full-slide normalization proved that no content outside the authorized `a:t` values changed. The master, all 11 layouts, theme, relationships, source placeholder formatting, and preserve-only slide remained byte-identical. Source-versus-edit rendering was identical outside the edited placeholder masks and slide 2 was an exact pixel match. Microsoft PowerPoint serialized a distinct file through forced `SaveAs`, reopened it, and retained the single master, named layout, four shapes per slide, footers, slide numbers, native text, and intended copy; both round-trip renders were pixel-identical to the edited deck. Five negative controls correctly rejected protected-theme mutation, preserve-only-slide mutation, same-slide non-target mutation, an unexpected package part, and a stale before-text contract. The pinned source SHA matched, creator fields were audited as neutral, two independent reviewers issued GO, and all six source, edited, and round-trip slides were inspected individually at full size with no clipping, overlap, or unintended drift. This establishes G10's narrow named-placeholder edit path; broad structural import remains uncredited under C10 and C17.
- 2026-07-15: Consolidated `npm run release:check` passed preflight, 28/28 tests, demo compile/font/lint/render/audit, the six-slide controlled fidelity benchmark and real PowerPoint group round trip, both copy-mutation builds, repair controls, independent image ingestion plus erased-text control, and the hardened golden-template path plus five tamper controls in 405.4 seconds. All 22 current demo, fidelity, mutation, ingestion, source-template, edited-template, and PowerPoint-roundtrip renders were then inspected individually at full size.
- 2026-07-15: The local G09 package now includes copy-ready submission text, a timed 2:45 audio demo script, five-minute judge instructions, a six-image screenshot manifest, reproducible asset hashing, and a binary submission checker. `npm run submission:prepare` passed locally, 31/31 tests passed, and the checker independently verifies every screenshot's exact path, byte count, and SHA-256. The exact clean-checkout judge sequence—clone, `npm ci`, runtime setup, preflight, demo, and independent ingestion—passed in 73.2 seconds. The full checker correctly remains red for the verified repository URL, public sub-three-minute YouTube video plus audio confirmation, primary `/feedback` session ID, verified GPT-5.6 statement in the final copy, Devpost participation, judge access, and unresolved verification tokens; therefore G09 remains `0`.
- 2026-07-15: C14 defect matrix v1 passed 47/47 unit tests and the `d48347e50ec2d45d9b964395c3acd94332073ec684320e3ba0532f0aa2f53dca` integration scorecard. Twelve isolated plan negatives and 14 total negative proofs cover canvas and parent clipping, independently recomputed text overflow, undeclared overlap, opaque occlusion, text and chart-mark contrast, compiler-declared alignment, wrapping, crowding, chart label size/collision, a false fit visible only after rendered layout, and an actual clipped PowerPoint. The clean three-slide deck and two native-shape chart-component slides passed plan lint, realized-layout lint, `slides_test.py`, and real PowerPoint bounds on all 21 text boxes; the deliberately clipped title failed PowerPoint bounds and the rejected PPTX was deleted. All five regenerated positive slides were inspected individually at full size, and an independent reviewer issued GO for C14=1. Native PowerPoint charts remain uncredited under C18.
- 2026-07-15: Consolidated `npm run release:check` passed the full preflight, 47/47 tests, demo compile/font/lint/render/audit, six-slide controlled fidelity and real PowerPoint group round trip, both copy-mutation builds, repair controls, independent image ingestion and erased-text control, golden-template round trip plus tamper controls, and the C14 plan/rendered/PowerPoint defect matrix in 493 seconds. Plugin and skill validators then passed. All 33 regenerated slide PNG outputs—including renderer previews, external renders, mutation variants, ingestion, template source/edit/round-trip, chart components, and non-deliverable false-fit previews—were opened individually at full size with no unintended clipping, overlap, or visual regression.
- 2026-07-15: C21's optional Windows adapter passed a fresh real-PowerPoint 16.0 round trip: it opened the generated benchmark, selected the named `invite-h-subtitle` shape, changed native text and bold from `0` to `-1`, ungrouped/regrouped, saved, reopened, retained the exact sorted set of all 16 named group members, and produced a distinct output SHA-256. A forced-unavailable control independently completed a fresh three-slide compile, zero-warning lint, native render, OOXML audit, and delivery verification with generation still enabled. The scorecard requires both `c21ProofComplete` and `optionalGenerationValid`; a skipped adapter cannot appear complete. Tests passed 52/52, all nine regenerated slides passed full-size review, and an independent reviewer issued GO for C21=1. The adapter remains a bounded Windows-only fallback and does not establish C16.
- 2026-07-15: Consolidated `npm run release:check` passed preflight, 52/52 tests, demo compile/font/lint/render/audit, controlled fidelity plus real PowerPoint group round trip, both copy mutations, repair controls, independent ingestion, golden-template preservation, the two-phase C14 defect matrix, and both C21 adapter paths in 557.6 seconds. Plugin and skill validators passed. All 42 current slide renders were inspected individually at full size with no unintended clipping, overlap, wrapping, or visual regression.
- Remaining primary release blocker: submission package (G09). Complaint-derived goals still retain independent binary evidence requirements.
- 2026-07-15: C16 fine-grained iteration v1 passed the content-addressed `fa4377c42c02100a26d58e221399408e536e0726b0941b4d58e1ba8da7e26be5` scorecard. Six stale-safe named edit manifests independently changed single-run text, run-level bold, color, position, a horizontal native-shape chart value, and a two-column gap with exact editor-derived closures. Deterministic normalization made two baseline exports byte-identical across all 33 package parts and 32 semantic relationship tuples; whole-package audits retained exact inventory, relationships/references, non-slide parts, named-object order, ZIP metadata, and zero pixels outside exported OOXML masks. The chart root and South mark both changed from semantic value 52 to 72 through hash-checked native shape descriptions with `officeChart:false`. Eight destructive controls rejected unauthorized objects, off-mask pixels, relationship-target drift, dangling references, master mutation, extra parts, chart-metadata tampering, and creation-ID drift. Microsoft PowerPoint 16.0 opened, saved, reopened, and preserved all 27 named shapes, semantic descriptions, formatting, and geometry in the baseline plus six variants; all seven post-PowerPoint renders were exact pixel matches. Tests passed 57/57, all 60 current benchmark slides were inspected individually at full size, and independent code/evidence and visual reviewers issued GO. This establishes C16 for bounded Slidewright plans only; native Office charts and arbitrary structural edits remain uncredited.
- 2026-07-15: Post-hardening consolidated `npm run release:check` passed in 1372.1 seconds, covering preflight, 57/57 tests, compile, font and layout linting, native rendering, OOXML/export audit, controlled fidelity, copy mutation, repair, ingestion, template preservation, defect gates, PowerPoint adapter, and C16 fine-grained iteration. Plugin and skill validators also passed. Three independent visual review passes inspected all 102 current slide renders at full size: 42 broader regression renders, 32 deterministic iteration renders, and 28 real-PowerPoint round-trip renders. Every suite received GO with no clipping, unintended overlap, corruption, wrapping, margin, typography, or edit-locality regression.
- 2026-07-16: G22 design-profile v1 passed on original synthetic, MIT-licensed PowerPoint fixture SHA-256 `0f1719c14b8a1fbc910c3b8f46cf5c8cf5e5a0b7ba9843a9e04ed23653dec5ed`. Two extractions were byte-deterministic and produced profile content hash `16225b3f3e99a11d0120b6e5d3aeb6014581928357d15cd27130ebdbb5b3771b`. The profile covers the 960x540 canvas, four exact guides, Arial typography and six integer sizes, five-color palette, one master, both named layouts, native grouped logo, placeholders, recurring chrome, and exact geometry. Direct and public-CLI derivations matched. The clone-only derived deck changed only the declared `SW Content Title` and `SW Content Body` `a:t` values. The plan-bound audit passed across the exact 39-part inventory, 36 relationship tuples, 26 protected parts, 81 chrome objects, four guides, five symmetry contracts, one group, three logo objects, and two authorized text shapes; every other package byte remained exact.
- 2026-07-16: G23 exact symmetry passed four source pairs: 2pt left/right content limiters, 2pt left/right section limiters, 4pt top/bottom rails, and 6pt left/right rails, with exact equal EMU thickness, opposite-edge offsets, colors, and styles. A separate 3pt/5pt divider pair was preserved only through a source-SHA and per-object-hash-bound asymmetry manifest and remained exact after PowerPoint SaveAs/reopen. Eight controls rejected guide deletion, a one-EMU rail drift, limiter-color drift, logo rename, theme-font drift, undeclared same-slide drift, profile tampering, and a visibly widened right rim; the rendered negative changed 30,190 pixels per slide at the right edge and failed exact comparison at 0.984588 similarity. Source-to-derived pixels were identical outside the two text masks, the untouched slide was exact, and both PowerPoint-roundtrip renders matched the derived slides at 1.0. Scorecard `cc5bdb072e737a6b2e636caf81de5fa4559ec14e14dd1bc0ec8eb402529d4c9c` is valid.
- 2026-07-16: The exact `npm run release:check` command passed in 1521.4 seconds with preflight, 67/67 tests, compile/font/layout linting, native rendering, OOXML/export audit, controlled fidelity, copy mutation, repair, independent ingestion, template preservation, defect gates, optional PowerPoint adapter, fine-grained iteration, and the G22/G23 design-profile suite. Plugin and skill validators passed. Three independent reviewers inspected all 112 regenerated slide renders at full size: 60 iteration, 42 broader regression, and 10 design-profile/negative-control renders. All positive suites received GO with no unintended clipping, overlap, wrapping, corruption, typography, margin, grouping, or edit-locality regression; the rendered rim negative showed only its intended right-edge widening.
- 2026-07-16: Direct user feedback from the `Locate event info` task established five additional binary requirements: text boxes never overlap; headlines consume full safe width absent a declared center or two-thirds split; background regions grow with wrapped text; every declared topic receives explicit structural coverage; and inherited empty paragraphs cannot create blank bullets or collisions. These are registered as G24-G28 at 0 until dedicated compiler, linter, OOXML, and rendered positive/negative proof exists.
- 2026-07-16: G24-G27 passed the content-addressed `locate-event-feedback-v1` benchmark with scorecard `3d4686a79e1ff261af4a11393e03010b5a2e61ed17ad9c907b0e037e4beae7fe`. Three deterministic compilations produced the exact 17-topic, 34-slide divider/content sequence and 110 native text shapes. Positive plan and realized-layout lint passed. Nine plan controls and five exported-OOXML controls rejected text/text overlap, text/reserved-region overlap, shortened no-split headline width, undersized title backing, missing divider, missing content, topic-order inversion, merged ownership, missing title, and empty-paragraph reinsertion. The suite proves 32 full-safe-width headlines, one declared center split, one declared two-thirds split, and nine visible multiline bands that grow to the realized text height with symmetric padding.
- 2026-07-16: G28 passed on the pinned PowerPoint-authored inherited-bullet fixture SHA-256 `99f48399b2766285a64e063894c6f864f130770be0115857cb6fc7146f73df5f`. The sanitizer removed exactly three empty paragraphs whose bullets came from the master body style, preserved the canonical hashes of all three non-empty native paragraphs, and changed only slide 1 XML; relationships, masters, layouts, theme, and the preserve-only slide remained byte-exact. Six destructive controls rejected stale input, wrong placeholder selection, blank-bullet reinsertion, non-empty deletion, same-slide title drift, and master-bullet drift. PowerPoint 16.0 saved, reopened, and retained the native placeholder, three visible bullets, zero empty paragraphs, exact state, and 1.0 rendered similarity.
- 2026-07-16: The exact consolidated `npm run release:check` command passed in 1619.3 seconds with 75/75 tests and every preflight, compile, font, plan-layout, realized-layout, native render, OOXML/export, fidelity, copy-mutation, repair, ingestion, template, defect, optional PowerPoint adapter, fine-grained iteration, design-profile, and feedback-contract suite. Stderr was empty. Plugin and skill validators passed. Three independent visual gates inspected all 390 canonical renders individually at full size: 108 positive feedback/template renders, 170 negative-control renders, and 112 broader regression renders. All issued GO. The negative review found exactly four visibly mutated targets, one pixel-invisible but structurally rejected empty-bullet target, 165/165 clean non-target slides, and zero mutation spread or unintended defect.
- 2026-07-16: The release repository is verified public at `https://github.com/mgritzbach/slidewright`; GitHub reports `main` as the default branch, public visibility, and implementation commit `959e46a88628c8b129d93bb93da3c42a0c4b594c` present on `main`. The authenticated GitHub API and installed GitHub app independently identify the connected account as `mgritzbach` (user ID 58284229); the requested `Hassanfroo` string was a stale local credential label and no exact GitHub user or organization exists under that handle. The full submission checker no longer reports a repository-URL failure, but still correctly blocks G09 for the missing public video and duration/audio proof, primary `/feedback` session ID, verified GPT-5.6 statement, Devpost participation, judge-access confirmation, and unresolved external verification tokens.
- 2026-07-16: Public Core CI run `29528766910` passed at commit `66e249442fd3b2bcdaf51c3fb01d07ea365f38d1` on independent GitHub-hosted Ubuntu and Windows runners. The initial public run correctly exposed the undeclared `lxml` clean-host dependency in both jobs; the repaired workflow pins `lxml==6.0.2`, pins Python 3.12, binds the pip cache to `requirements-ci.txt`, uses current Node-24-based checkout/setup actions, and grants read-only repository contents permission. Both jobs then passed dependency setup, 75/75 tests, demo compilation, and zero-warning lint. C22 remains `0` because the public workflow does not yet publish versioned scorecards and full evidence artifacts or constitute the complete fresh-machine replication report required by that goal.
- 2026-07-16: C22 public-evidence v1 passed. Three committed capable-host scorecards bind C14, G22/G23, and G24-G28 to exact commands, explicit limitations, 36 destructive controls, file hashes, and manifest `6d0ae8df8eb04d190235868e458c43957dc477b00dee10fd0764bf1a3636d01d`; the publisher rejects invalid suites and machine-specific paths, while the verifier also refuses drift from current generated outputs when those outputs exist. Public GitHub run `29530184850` at commit `bdcb6c94beb5c0bd206d789bf63b5c23d96f2830` ran clean `npm ci`, pinned Python setup, and `npm run evidence:ci` on independent Linux and Windows hosts. Each passed 80 tests, all 15 required portable destructive-control tests, a three-slide compile, zero-error/zero-warning lint, and all three committed scorecards. Artifact IDs `8388299620`, `8388312071`, and `8388319066` are bound to GitHub SHA-256 digests in artifact-manifest `0b7f182609ec620a4257c3ee708b77c3b02797bd24a2f8b0102c8030a1d022ea`. The third CI job downloaded both host artifacts, required the same commit, evidence manifest, and platform-independent result hash `d6e16366a868e7e558f45f95ea28b1f32932b5450a6be3fe5d67d69ce9984753`, and emitted aggregate `a57bf6eeb0ccef28d7eb1a5f0496aee83a0f28a5104702488dd013314811f00e`. Those immutable host scorecards and the replication report are committed under `evidence/c22/v1/`; local redownload and aggregation reproduced the exact aggregate hash. Follow-up public run `29530453087` passed all three jobs with the committed artifact verifier active. PowerPoint/Codex-runtime stages remain correctly labeled as capable-host snapshots rather than falsely claimed GitHub-runner execution.
- 2026-07-16: C08 semantic-surface v1 passed with content-addressed scorecard `c37f1786955eb2454bd430df72563c042f559a417f6897b2a2987db8637081ac`. Three exports were byte-identical at PPTX SHA-256 `e70684fc0238f663ed0eab42b3d1dacd5f99dd2947ec436cb96362195b0f908c`. The frozen four-slide contract matched all 40 declared semantic objects: 32 native shapes with exact text-bearing hashes, two nested groups with six nested objects, two native Office chart parts with exact cached directions/categories/series data, one native 20-cell table with exact matrix hash, two attached connectors with exact endpoints, one declared image with exact media hash and alt text, and four meaningful notes slides. Nine destructive controls rejected broken chart relationships, chart/table flattening, detached connectors, stripped notes, flattened or drifted hierarchy, image relationship drift, and an undeclared object. Microsoft PowerPoint 16.0 build 20131 SaveAs/reopen retained the exact top-level state and produced round-trip SHA-256 `fd3e6e2233a9af6c694d1fb48398fc6f6820a80e6b2ce960e0678f5fc7632d93`; the bounded relationship-rebase audit retained every frozen semantic field. All ten full-size render sessions used newly owned PowerPoint processes. A deliberate five-second hung-worker control recorded the exact owned PID/start time, terminated the worker, removed the non-child COM process, and verified that no PowerPoint process remained. Source and round-trip PNG/JPEG hashes matched slide by slide, and two persisted `slides_test.py` reports found no overflow. All eight fresh source and round-trip review slides were inspected individually at full size with no clipping, overlap, wrapping, margin, chart, table, connector, or media defect. This credits controlled semantic representation and preservation only; chart-data editing, table-cell editing, arbitrary existing-deck ingestion, and general native-object mutation/readability remain uncredited under C17 and C18.
- 2026-07-16: The preceding C08 scorecard is retained as historical evidence but is no longer current proof. Independent review found that the older timeout/cleanup implementation could attach to or terminate a user-routed PowerPoint session, and an interrupted rerun removed the local scorecard before replacement. The hardened implementation now rejects pre-existing sessions before COM creation, proves the exact `/AUTOMATION` PID/start time, never calls PowerPoint `Quit` or force-kills `POWERPNT`, closes only captured allowlisted hidden worker presentations after immediate identity checks, safe-refuses visibility/foreign-object/collection drift, kills only exact worker processes, survives forced parent termination through a detached watchdog, and publishes immutable versioned runs through an atomic current pointer. The empirical mismatch/no-kill, forced-parent watchdog, and versioned-publication controls pass, 89/89 tests pass at this checkpoint, and two independent reviewers issued GO on the final safety logic. Because the exact safer implementation still needs a fresh full PowerPoint scorecard and full-size render review, C08 is reset to `0` rather than inheriting the obsolete scorecard.
- 2026-07-16: C18 implementation reached the static-evidence checkpoint without touching the active user PowerPoint session. The source-bound five-case mutation contract, native PowerPoint save/reopen worker, fail-closed semantic/readability auditor, render-derived chart measurement, immutable versioned runner, and nine intended-code destructive controls are implemented. A fresh non-PowerPoint export retained all 40 semantic objects and passed the authored contract; chart labels now export at 12pt, table cells at 15pt, both series exceed 3:1 mark contrast, the horizontal labels render without truncation, and render measurement found all eight marks with 51px/81px minimum thickness plus in-bounds non-overlapping labels. The stale prior deck was deliberately rejected for 9pt chart labels, sub-14pt table text, low teal contrast, stale semantics, and missing C18 PowerPoint evidence. The repository test suite passes 96/96, demo compile/lint/render/audit pass with zero warnings and zero pictures, all three demo slides and the revised chart/table fixture slides were inspected individually, and plugin/skill validation passes. C18 remains `0` until the five mutations run through fresh real PowerPoint save/reopen, all nine controls reject, all 24 mutation renders are reviewed at full size, and the versioned scorecard exists.
- 2026-07-16: Post-review C08/C18 static hardening closed the remaining identified safety and audit gaps without launching or touching PowerPoint PID 34308. All four workers now publish exact PID/name/start worker intent before COM creation, require two-sample hidden/empty ownership, and guard every presentation close with immediate allowlist, visibility, captured-object, and stable-inventory checks. Direct timeout/signal termination exact-matches the worker PID/name/start and explicitly refuses `POWERPNT`; the detached watchdog repeatedly reconciles delayed intent and ownership records, derives safe/recovered validity, and detects new orphan PowerPoint processes. Content-addressed publication now byte-compares an existing run before accepting replay and rejects a tampered hash directory. C18 now compares canonical raw-object and protected-package signatures with operation-specific masks, exact connector sites, PowerPoint-reported data-label bounds, static table fit, contract-bound before/after/reopen state, and structured malformed-render failure. A separate finalizer requires exactly 24 individually recorded full-size review decisions and binds them to both PNG and review-image hashes outside the immutable machine run. The no-PowerPoint suite passes 101/101 tests, including delayed-intent recovery and tamper controls; demo compile/lint/render/audit pass with zero warnings, 18 native text nodes, zero pictures, approved integer type, and mixed emphasis; all three demo slides were inspected individually at full size; plugin and skill validation pass. The C18 command safe-fails before staging or COM when no current hardened C08 baseline exists, leaving the active PowerPoint PID unchanged. C08 and C18 correctly remain `0` until fresh real-PowerPoint runs and bound full-size review evidence exist.
- 2026-07-16: Hardened semantic checkpoint `ed3eacfea457c0b5efc26bc76a4d10c4bc600843` is published on public `main`. GitHub Core CI run `29543569421` passed the exact commit: independent clean-install Windows and deterministic jobs completed dependency setup, `npm run evidence:ci`, and artifact upload; the cross-platform aggregation job downloaded both host artifacts, reproduced the aggregate, and uploaded the result. Independent static review issued GO to begin the sequential real C08 then C18 runs, with both goals still held at `0` pending their required PowerPoint and visual evidence.
- 2026-07-16: C12 prompt-robust workflow v1 passed with content-addressed machine scorecard `ddf80b3cb01d4693fae381e4ba9280517da57244b8b0eedb25931c72eb9b83fd` and separately bound full-size review `16109601c7dc5d43e580647ff15c3d0484d4f6b40d00a5d3fcb42ee988f86ab8`. The committed 12-case corpus covers two minimal, two verbose, four conflicting, and four adversarial prompts over three repeats: all 36 outcomes matched, 15 accepted requests produced native editable PPTX files, and 21 requests were rejected without publishing a plan or deck. Minimal and verbose semantic pairs produced byte-identical plans and normalized decks; every accepted run independently reclosed the immutable policy, compile, font, lint, render, generic audit, plan-bound audit, and delivery stages. Fourteen destructive controls rejected missing or reordered stages, forged reports, relaxed plans, fractional type, overlap, substituted decks, prompt tampering, artifact drift, and rejected-run deck injection; eight injected stage faults left no deliverable artifacts. The three unique decks' nine PNGs were individually inspected at original resolution and passed without clipping, overlap, crowding, hierarchy, padding, background-growth, or emphasis defects. Independent release review reran the complete benchmark in 483.5 seconds, rehashed the implementation, fixtures, scorecard, review, and all previews, and issued GO. The repository suite passed 118/118; demo compile, font audit, lint, render, and OOXML audit passed with zero warnings, 18 native text nodes, zero pictures, approved integer type, and mixed emphasis; plugin and skill validators passed. This credits the strict request/build boundary and committed corpus only, not universal natural-language understanding.
- 2026-07-16: C12 implementation commit `5cb2f518819f3ef82dd93042d507f8b0f5edf48f` is published on public `main`. GitHub Core CI run `29546675310` passed that exact commit: the deterministic and clean-install Windows jobs both completed dependency setup, the 118-test `evidence:ci` gate, and artifact upload; the aggregation job downloaded both host artifacts, reproduced the cross-platform evidence, and uploaded the aggregate.
- 2026-07-16: C15 dense-content and copy-mutation resilience passed with content-addressed machine scorecard `588a74444379d288ddbb833329b14d6976991cfd945be0e3ed1f21b05fe847e2` and bound full-size review `96de82a5667e46285ac7260b30f34eb3e6689450dec11f23a605569516216826`. Five pinned cases cover -25%, +25%, a human-authored German expansion, 2x flexible copy, and 4x flexible copy. The dense fixed three-slide plans fail with `SW004`/`SW013`, while deterministic structural adaptation succeeds at 3-to-4 and 3-to-9 slides without clipping, sub-minimum or fractional type, pictures, or lost normalized word/style records. All 831 normalized word tokens retain order and bold, italic, color, bullet, and level state; whitespace and source run boundaries are explicitly allowed to normalize. Nine plan/spec controls bind their exact CA diagnostics and, for plan defects, the intended `SW004`, `SW002`/`SW009`, `SW003`, and `SW018` rules. A real nine-picture, zero-native-text PPTX fails both OOXML auditors. A production guarded request exceeds the actual 200-slide ceiling at compile and publishes only inert request, policy, and run receipts. The immutable run contains 104 hash-bound artifacts, including every source/adapted specification, fixed/adapted plan, lint/audit report, PPTX, and review PNG. The standalone verifier independently derives the exact workspace dependency closure, binds the 13-file presentation runtime tool tree, complete 125-file `@oai/artifact-tool` 2.8.24 tree, Python package versions, current human review, and all artifacts. All 22 PNGs were inspected individually at 1280x720; independent visual and release reviewers issued GO. The repository suite passed 129/129, demo compile/font/lint/render/audit passed with zero warnings and zero pictures, and plugin/skill validation passed.
- 2026-07-16: C12 was requalified after the final adaptive-copy integration rather than inheriting an older implementation hash. Machine scorecard `0747ecf75a92fefa8fd9aae0eac90d024c3ccce8b771b898567963182f994b0c` reran all 36 prompt outcomes, 14 destructive controls, and eight injected fault controls against the exact current request pipeline; every expected build/rejection and immutable stage closure passed. Its nine current review PNGs are byte-identical to the individually inspected images and are rebound by review `12de2a3356828292145b1b15b4b455cd89538352ba8c15281528adbed037af7d`.
- 2026-07-16: C15 implementation commit `fcf8d935ac3258316746c7e2d29280e00bc906ca` is published on public `main`. GitHub Core CI run `29550476386` passed that exact commit: deterministic Linux and clean-install Windows jobs completed dependency setup, the 129-test portable `evidence:ci` gate, and artifact upload; the aggregate job downloaded both host artifacts, reproduced cross-platform evidence, and uploaded the result.
- 2026-07-16: C15 was requalified after its standalone verifier exposed that the prior machine scorecard predated a root `package.json` change. Current scorecard `94e6af1c16d2ec1680364d47d9855fccd7305e410e36175cf195ea025297ef85` passes five cases, 10 destructive controls, 104 bound artifacts, and review `74f7cac13453bf9e370cef6afd03b6df37e23de67cd55755dd29beacd92e612a`. All 22 regenerated review PNGs are byte-identical to their previously inspected counterparts; an independent visual reviewer rechecked the current hashes and representative full-size expanded, translated, dense, and continuation slides and issued GO. The Codex installation harness now has its own pinned package and lockfile under `tools/installation/`, so future Codex harness upgrades do not mutate the presentation runtime dependency graph.
- 2026-07-16: C02 reached a local implementation checkpoint but remains `0` pending exact-commit public replication and the stated client-surface proof. A fresh isolated `CODEX_HOME` used pinned `@openai/codex` 0.144.5 to add the marketplace, discover the available plugin, install it through the real CLI, verify the canonical installed path, and reproduce the exact 95-file source tree. Separate outside-repository app-server sessions using Desktop and VS Code client identities each found enabled `slidewright:slidewright`; these are backend probes, not GUI-launch claims. Thirteen C02 tests reject missing file receipts, malformed tree hashes, fabricated argv, removed controls, non-checkout commits, forged GitHub provenance, unsafe or overlapping cleanup paths, path-prefix escapes, platform line-ending drift, and a hung launcher/descendant tree. Windows cleanup captures PID/name/creation-time identities, terminates only the owned tree, verifies no descendant survives, and propagates every client cleanup failure. The full suite passes 142/142, demo compile/lint pass with zero warnings, public and C15 evidence verify, plugin/skill validators pass, and independent release review issued implementation GO. Local scorecard `d7800666b1a20d21dbf87732917b7b1879b20c3d4688ef2291e68bea52c90556` is valid; it is not cross-platform release credit.
- 2026-07-16: C02 public backend/CLI replication passed without changing its binary status. GitHub Core CI run `29554999630` passed exact commit `0941043ea51219c77da4aee6197ca8736d8f06e3`: Linux x64, Windows x64, and macOS arm64 each installed pinned `@openai/codex` 0.144.5, ran all 13 C02 tests, installed the public exact-ref plugin through the real CLI in a fresh home, discovered the enabled skill through Desktop- and VS Code-identified app-server sessions, and uploaded raw command logs plus scorecards. Aggregate artifact `8397106417` has GitHub digest `8b88b17c6e8ce1141bf60261f927fba7939540aadc5de6a0d27963c7968d4d63`; its scorecard SHA-256 is `3d6b72cd48976297cba254c36473b9ad835bf7d2cfa654e25296f964d7547aa3`, aggregate hash is `390591d935f161de6fcc4ffff14bbb441a7403b807e5e842582e253345ea5ff0`, and all hosts installed byte-identical plugin tree `d2e8bb9333e5c9e1e303fb88deefbfee9cbc5dc8cc75713f69e2362647c5c62d`. Redownloaded host artifacts reproduced byte-identical aggregate JSON and Markdown locally. Two independent reviewers issued NO-GO for C02=`1` because caller-supplied app-server client identities do not prove discovery inside actual Codex Desktop and VS Code extension sessions. C02 therefore remains `0`; the completed evidence is explicitly limited to real CLI installation and shared-backend compatibility.
- 2026-07-16: C03 reached an independently approved implementation checkpoint while remaining `0` pending public exact-commit proof. The deterministic resolver accepts only supported local explicit paths, an already valid workspace package, or Codex's bundled dependency runtime; explicit overrides fail closed, packages are smoke-imported exactly once, post-link resolution is realpath-bound without a second evaluation, and no helper, network, package-install, or renderer-switch path exists. Corrupt/lookalike packages, directory entrypoints, malformed or prerelease versions, symlink escapes, workspace collisions, missing runtimes, invalid overrides, forged command receipts, tree drift, provenance drift, and destructive rollback cases all reject. The targeted suite passes 23/23 and the repository suite 159/159, including macOS `/var` to `/private/var` realpath normalization. The real Windows benchmark passes all five controls against `@oai/artifact-tool` 2.8.24 with a 125-file tree, exit `0`, and empty stderr; clean-host and invalid-override targets remain nonexistent. Demo compile/lint/render/audit passes with zero warnings, 18 native text nodes, zero pictures, integer approved type, and mixed emphasis; all three slides passed individual full-size review. Plugin/skill validation, public evidence verification, and refreshed C15 evidence `a41e567d5c8c58de9b8b5743450780f756812f4c6e17b99369b588cf6b2c8c08` pass. Two independent reviewers issued implementation GO. Native aggregation is forced non-creditable with WSL pending; complete aggregation requires exact matching implementation/commit across Windows, macOS, Linux, and genuine WSL, then independently fetches and byte-compares public same-repository WSL scorecard and command-log release assets. C03 stays `0` until those public native and WSL proofs exist and the complete aggregate is reproduced.
- 2026-07-16: C03 public release proof passed and the goal advanced to `1`. Exact implementation commit `0b4bafbff9b783ee95af76ffba8612e1893819ac` passed Core CI run `29558000902`: native Windows x64, macOS arm64, and Linux x64 benchmark jobs plus native aggregation all succeeded. Host artifact IDs `8398160024`, `8398156592`, and `8398156656` have GitHub digests `1076412041aa59c19c8d0b04b4243ce781777669129aacc45625befd0c7d5af9`, `10b7a9f42b607afc2f5a21137e68c4358df49a3af745b284d7aab9b812986dc1`, and `94a47878d17d8dcfd8d7f55d7579c0a4c659bf651112afe78894a392a27efa2`. Native aggregate artifact `8398163819` has GitHub digest `04d052ded5d6864ab17c63944d6c7a399d8003284f4cd6609d88b0c693cca9e8`; a fresh local reconstruction using the CI artifact directory layout was byte-identical at file SHA-256 `df7376006b4d58f47b83af83e6290ca9a86a19600d20a2e014fa8c4bce5fa00` and correctly remained non-creditable with WSL pending. Genuine WSL2 evidence was then generated inside `docker-desktop` on Linux x64 kernel `6.6.87.2-microsoft-standard-WSL2`, detected without override with Microsoft `/proc/version` and filesystem evidence, and published in release `c03-runtime-0b4bafb` targeting the exact implementation commit. Public scorecard asset `480021081` and command-log asset `480021082` have SHA-256 `66b57e0e6a2597824781d03eb8ff0ad32f6459c7449cdfd165ba682ffae6ce8c` and `18f6e43f0d0e5d72e4e69165ecf154be3da265b206eb2ba7198ba2855c841419`. Complete aggregation required all four matching profiles, exact commit/contract/implementation hashes, same-repository provenance, and independently fetched and byte-compared both public WSL assets. It passed twice byte-identically with `complete=true`, `creditEligible=true`, no pending profiles, content hash `abe3d1760e105663525db2a054a8044157bed1a81cc5a4a3b0ced95d4696b125`, and file SHA-256 `19b61c9422ce24ad71f87fc73293cd64180b72fd805d4a4f41028aba0bd6cfcc`. Independent final evidence review redownloaded all five public inputs, reproduced their hashes and aggregate, verified the genuine WSL identity, and issued GO for C03=`1`.
- 2026-07-17: C08 was requalified against hardened implementation commit `bdc971d0b518a3a7db166fc8d8d950fc970b0d45`. Fresh real-PowerPoint scorecard `6550cfa3221e31fe04af27cc8cd915a6a45e39bab6279b971e1c95c5cad639d7` passed in 313.7 seconds and independently reverified with 127 exact immutable receipts. The run snapshots every implementation/input file, including the declared raster asset; historical verification resolves the contract, source asset, scripts, and exact receipt inventory from that recorded snapshot rather than mutable live source. All semantic audits, nine destructive controls, process-quiescence gates, exact worker-identity cleanup, deterministic exports, PowerPoint SaveAs/reopen, two overflow checks, and source/round-trip render hashes passed. Root and independent reviewers inspected all eight 1600x900 JPGs individually, one image per visual-tool call, and issued GO with no clipping, overlap, wrapping, margin, chart, table, connector, or media defect. Raw PNG/JPEG prefix crops were topology-valid and byte-identical across all eight source/round-trip renders. C08 advances to `1`.
- 2026-07-17: C18 native-object mutation v2 passed against the exact C08 baseline above. Fresh real-PowerPoint scorecard `283a53b3cac5875453d76098c08522bf0fabda243b0255e1d203ec3e369e9f54` completed in 773.2 seconds and independently reverified with 280 exact immutable receipts. PowerPoint edited and reopened horizontal chart data, vertical chart data, a native table cell, a grouped diagram node, and attached connector geometry/style; all five semantic/readability audits, nine intended-code OOXML destructive controls, six overflow checks, seven interstage quiescence gates, 48 PNG/JPEG rendered-header checks, immutable C08 glyph-reference matches, and four rendered-header destructive controls passed with zero warnings. Root review `b5bd601083c777b01c6f3f1e5b38a683bceeaaeef34627a24079b1927fb6c78c` binds 24/24 full-size pass decisions to exact PNG/JPEG hashes. A separate reviewer independently inspected all 24 1600x900 JPGs one image per call and issued GO; all 15 non-target mutation slides were byte-identical to baseline and only the five intended target slides changed. C18 advances to `1`.
- 2026-07-17: Post-credit release gates passed: 190/190 repository tests; demo compile and lint with zero errors and zero warnings; native three-slide render; OOXML audit with 18 native text nodes, zero pictures, approved integer font sizes, and mixed bold/regular runs; plugin validation; and skill validation. All three regenerated demo slides were inspected individually at original resolution and passed with no clipping, overlap, bad wrapping, asymmetric padding, or visual regression.
- 2026-07-17: Clean-commit consolidated `npm run release:check` passed at `33a49e3` in 2,173.5 seconds. The one-command path completed preflight, clean runtime bootstrap, 190/190 tests, demo compile/font/lint/render/audit, controlled fidelity, copy mutation and adaptive resilience, repair controls, independent image ingestion, template preservation, geometric/readability defects, optional PowerPoint adapter, fine-grained iteration, design-profile/rim symmetry, feedback contracts, prompt robustness, hardened C08 semantic surface, and public evidence verification. The replay produced C08 scorecard `7a8bf20dd5ec127ceb162a24356b658afcb240e8127162b53659e120d3056109`, which independently verifies with 127 receipts; all 16 of its PNG/JPEG source and round-trip renders are byte-identical to the previously reviewed C08 proof. The retained C18 scorecard and bound review independently reverified after the release run with 280 receipts and 24/24 slides, and no PowerPoint process remained.
- 2026-07-17: Windows Core CI run `29578326781` correctly blocked commit `73ab7ceceeb9283e93cdb336662b5eec98358fd7`: its retained `tests.log` proved 190 total, 188 passed, one failed, and one skipped. The sole failure was the C08 clean-Git control comparing two lexical spellings of the same hosted-runner temporary directory; the scorecard also mislabeled the TAP total as its pass count. Fix commit `a43b722d39df0f8aa02140d3fcafeedfb4dbd4fa` canonicalizes both existing roots with native realpath and Windows case folding while retaining exact repository-root, 40-character commit, and empty-status requirements; a nested-directory control proves the gate was not relaxed. Fresh-host evidence now parses and separately gates total/pass/fail/cancelled/skipped summaries for both Node 22 TAP and Node 24 spec output, records child signal/error, and binds every raw log by relative filename, byte count, and SHA-256. Targeted tests passed 30/30, `npm run evidence:ci` passed with 191/191, and both independent reviewers issued GO on the diagnosis and fix.
- 2026-07-17: The exact clean fix commit `a43b722d39df0f8aa02140d3fcafeedfb4dbd4fa` passed consolidated `npm run release:check` in 2,226.6 seconds. The full gate covered preflight, runtime bootstrap, 191/191 tests, demo compile/font/lint/native render/OOXML audit, fidelity, copy mutation and adaptive resilience, repair, independent ingestion, template preservation, geometric/readability defects, the optional PowerPoint adapter, fine-grained iteration, design-profile/rim symmetry, feedback contracts, all 36 prompt-robustness outcomes plus controls, fresh hardened C08 semantic evidence, and public evidence verification. C08 scorecard `5e06b5897449d50899400fb3ec28b78382f50b387fef8aa56a61117737989194` binds clean commit `a43b722`, implementation hash `1afa630b581a26ad81b2ace157cd0fb1fa56554466d37e9fc2858fb5cdeb3c9c`, 127 immutable receipts, nine rejected semantic controls, exact PowerPoint 16.0 build 20131 source/round-trip render parity, two valid overflow audits, and four exact interstage/terminal quiescence checks. All 21 regenerated PNG/JPEG and declared raster assets are byte-identical to the already individually full-size-reviewed run `7a8bf20dd5ec127ceb162a24356b658afcb240e8127162b53659e120d3056109`; plugin and skill validators passed, and no PowerPoint process remained. Binary status remains 42/50 pending public exact-commit CI and the eight independently defined unmet goals.
- 2026-07-17: Public Core CI run `29582598574` passed exact commit `b0cce695354a9d43126cc2d2007737aee893faf0`, closing the Windows path and cross-platform scorecard regressions. Deterministic Linux and Windows evidence jobs, Linux/Windows/macOS installation and runtime-bootstrap matrices, both subsystem aggregates, and final cross-platform aggregation all passed. Linux recorded 192 total, 183 passed, zero failed/cancelled, and nine platform skips; Windows recorded the same 192 total, 191 passed, zero failed/cancelled, and one platform skip. Both independently proved all 15 required tests as actual passing lines, three demo slides, zero lint errors/warnings, exact four command receipts, raw regular non-symlink log bytes/SHA-256, and shared portable-result hash `4a49093285493e6af1e22e5030be0bb95394af9a51c89f51e0e667dda9a45160`. Aggregate artifact `8407653765` has GitHub digest `a59866126bed338cd10dfcd553944f31c8a6e38c30d901a4fcaa13b2f428a2cf` and content hash `b0f6498615bcb8bf332a046cee3c2535a320a622d40030016289e0bc9864f3db`; host artifacts `8407630885` and `8407648569` have GitHub digests `9d115b66c1a944422d8d9cb77be2be07b4b6168b842ed8ce948a05415f8a5bfb` and `2671509ee8ca43ade28f373ddfb70cd782f0017731dcf5ea999b0cebdce59fbb`. A fresh local aggregation from the downloaded host artifacts reproduced the public aggregate byte-for-byte at file SHA-256 `3a2ab317ee2eb57be7deb9d17f744a3780bbadde4d2b8104cce5e5209be2370d`. Two independent reviewers issued final GO after exploit controls rejected raw failures, skipped required tests, altered log bytes, symlink/path escape, missing/null/string/negative lint fields, command drift, and portable-result drift. Binary status remains 42/50; this restores release-green public evidence without claiming an additional goal.
- 2026-07-17: G29-G35 universal-design contract passed. `npm run universal-design` rebuilt seven slides across six unrelated page archetypes three times deterministically, passed plan lint with zero errors/warnings, rendered 45/45 named native objects, matched 33/33 text objects and 35/35 native paragraphs, matched one native table with exact 8px four-side cell margins and 16pt header/body text, matched four ontology-bound editable icons with embedded semantic metadata, and emitted zero pictures. Twenty-one isolated plan mutants reject overflow, 6+6pt stacked spacing, declaration removal, attempts to weaken built-in archetype/typography/component/backing/icon/inset/paragraph contracts, and injection or rebinding of custom generated archetypes and typography roles through SW004 and SW023-SW029. Seven exported-OOXML mutants reject text/table insets, paragraph spacing, icon metadata, a backing shrunk far enough to cause visible text spill, repeated-component style, and headline size. Seven hash-bound 1280x720 baselines were each inspected individually at full size, including a second same-family/same-variant comparison slide. Guarded copy adaptation compiled every archetype, preserved native table semantics, and split a 45x single icon-card body into three chunks/two continuations while all four peer bodies retained one 16pt style. The screenshots supplied by the user were used only to define generalized failure classes; none were copied into fixtures or committed.
- 2026-07-17: Final hostile re-review independently reran direct metadata deletion, second-order built-in contract weakening, custom archetype/typography/icon injection, 6+6pt spacing, single-card copy stress, and exported spill/style/size attacks. All intended attacks were rejected, the clean plan produced zero diagnostics, and the reviewer issued GO for G30-G35. The full repository suite passed 227/227, demo compile/lint/render/audit passed with zero lint warnings and zero pictures, all ten rendered demo/universal slides were reviewed individually at full size or matched their reviewed hash baseline, and plugin plus skill validators passed.
- 2026-07-17: Public Core CI run `29608143378` passed exact universal-contract commit `168e45552772803f3132560dfdafedf3d6bb31bb`. All 11 jobs passed: deterministic Linux core, Windows core, three native runtime-bootstrap profiles plus aggregate, three installation profiles plus aggregate, and final cross-platform evidence. This closes exact-commit publication proof for G29-G35 without crediting the unrelated unfinished repair-free workspace changes.
- 2026-07-17: G36 E6 executive review passed. The guarded request schema accepts only `off` or `executive-overlay`; invalid modes reject before compilation. The three-slide on/off benchmark independently verified both request runs and produced byte-identical canonical PPTX SHA-256 `33bd076dcde5b5d95d2adef78db16a295149a13caa0f7925ec90cd98b335bf10`. Off mode emitted no review deck and the clean-deck audit found zero `SW-E6-*` objects. On mode emitted a separate `deck.executive-review.pptx` with five concise, target-bound yellow partner checks across all three slides; the OOXML audit matched every exact object name, slide, note, fill, border, and native text body with zero pictures. Review notes were non-overlapping, inside the canvas, editable, and removable. All three annotated and all three clean demo slides were inspected individually at original resolution. Repository tests passed 232/232; demo compile/lint/render/audit passed with zero warnings, 18 native text nodes, zero pictures, approved integer type, and mixed emphasis.
- 2026-07-17: C04 reached a fresh development-functional checkpoint but remains `0` pending a clean, producer-regenerated release run on the final commit. Development scorecard `8289bee7a12a05914a412c2344beffe331d54b433f491a9ccc3e9ba992800d1c` passed 26/26 byte-unique fixtures and 13 controls, then independently reverified from its published evidence tree. The real repair-dialog control observed 151 persistent same-PID `NUIDialog` samples, dismissed only the exact recorded modal handle with `WM_CLOSE`, verified the handle disappeared, allowed the exact worker and PowerPoint process to exit naturally, and restored global PowerPoint quiescence without `WM_QUIT`, process-wide termination, or a PowerPoint kill. The watcher report is hash-bound to the exact ownership record and live worker identity; the timeout marker is preserved through shutdown; modal dismissal has its own raw stdout/stderr command receipt; forged, stale, unbound, non-modal, multi-window, identity-drift, and unsafe-cleanup paths fail closed.
- 2026-07-17: E6 stage integration was propagated into the C12 release benchmark after the first clean C04 producer run exposed a stale seven-stage scorecard literal. The benchmark now consumes the shared immutable stage contract for exact closure and fault injection, removes every E6 artifact on injected failure, and documents the guarded `reviewMode` envelope. The corrected matrix passed 36/36 request runs, 14 destructive controls, and nine stage-fault controls at scorecard `0c81cc16a8ee405be760a222a51f3a83009bc8dddacfd64a67de2ad7a63ac66b`; the repository suite passed 233/233 and the required demo/OOXML/plugin/skill gates passed. C04 remains `0` until a new clean producer-regenerated repair-free release run succeeds on the final commit.
- 2026-07-17: A later clean C04 producer run exposed a live-executable capture race after an isolated PowerPoint render process exited between ownership publication and the 100ms orchestrator poll. The semantic-mutation and isolated-render workers now use an exact PID/name/start-time runtime-capture acknowledgement bound to the executable receipt SHA-256; each owned process remains live until that acknowledgement arrives or fails closed after 30 seconds. PowerShell parsing, 24 focused tests, 234/234 repository tests, demo/OOXML/plugin/skill gates, and the complete real-PowerPoint semantic-mutation producer passed. Scorecard `234fe39a7379e4744cbfcb473523cf957b1a3b1d6f3fa57d751ed424ce6a3f56` closed after 634 seconds. C04 remains `0` pending a clean repair-free release run on the final commit.
- 2026-07-17: The next clean C04 producer run proved the isolated-render handshake also needed to be implemented by the C08 semantic-surface orchestrator that shares the worker. Both semantic orchestrators now capture every changing PowerPoint PID/name/start identity, resolve and hash its live executable, publish the exact acknowledgement, require the expected session count, and remove temporary handshake artifacts before evidence collection. The combined 49 ownership/watchdog tests and 235/235 repository tests passed, followed by the complete real-PowerPoint semantic-surface producer at scorecard `b1dc15015144232ca49410f41bace2bb483b566906360c03198623664f452b31` in 246 seconds. C04 remains `0` until the full clean repair-free release closes on the final commit.
- 2026-07-17: C04 advanced to `1` on clean pushed commit `be56316`. Release mode freshly regenerated every upstream producer and then passed 26/26 byte-unique fixtures plus 13 destructive controls in 2,448.1 seconds at scorecard `99f50f10bdcf8c5e81a1ba36fe44630bda014b9195726c92adbdafcdfb15b676`. The independent `npm run repair-free:verify` pass rederived package/relationship/content-type, pinned Open XML SDK, semantic state, real-PowerPoint open/save/reopen, watcher, exact modal dismissal, ownership cleanup, command, implementation, source, and quiescence evidence. Final verification found zero live PowerPoint processes. Complaint credit is now 16/22 and total credit 51/58.
- 2026-07-17: Codex client compatibility release `0.2.1` is published on public `main` at commit `242aba94606cdc64f9d0f826cdcfa9af87272ef2`. A real OpenAI VS Code extension session had exposed that Codex ignores an entire `interface.defaultPrompt` array when it contains more than three entries. Slidewright now ships exactly three prompts and hard-fails its installation benchmark for more than three prompts, prompts over 128 characters, empty or duplicate prompts, or drift among the evidence contract, `package.json`, both `package-lock.json` version markers, and the plugin manifest. Local release gates passed 238/238 tests, demo compile, zero-error/zero-warning lint, plugin validation, and skill validation. Exact-head push run `29626667608`, pull-request run `29626672241`, and post-merge `main` run `29626740409` all passed every core, native runtime-bootstrap, Linux/macOS/Windows installation, aggregate, and cross-platform evidence job. Main installation aggregate artifact `8424111154` has GitHub digest `5401c2138a8b73ca7516ae148e2642b600c3ed3ac187b96301ab285a5481972d`, content hash `5db84fb55a5b75d7ce731b5a233cd74fe069447e8077f121e41b1dbfc4c54b98`, and byte-identical plugin tree `1285d1a532773cf6e5058306f25bd9d6d09096183f31deebe7145b1c9543e6a8` across all three hosts. A fresh outside-repository Windows workspace and isolated Codex home then installed the exact public commit and version `0.2.1`; genuine VS Code 1.129.1 with OpenAI extension `26.707.91948` activated, spawned its Codex app-server, and initialized without the former Slidewright manifest warning. C02 deliberately remains `0`: the isolated extension session did not complete a client-originated nonce task proving `slidewright:slidewright` was injected and usable, and the active Codex Desktop process was not restarted to provide equivalent actual-client proof.
- 2026-07-18: G09 remains `0`, but its model/session evidence is now resolved. The primary Codex Desktop rollout `019f6485-a254-7b30-8da7-f26d254bfcb8` records `model: gpt-5.6-sol` and `effort: high` in its turn context. The same exact statement and session ID are now in the README, submission copy, and metadata, and Devpost draft `1087402` is recorded as joined at step 4/5. A corrected 1920x1080 narrated demo was rebuilt with explicit “GPT-5.6 in Codex” audio; it is 172.93 seconds, H.264 plus stereo AAC 48 kHz, mean volume -19.6 dB, peak -1.5 dB, and local SHA-256 `ead48cddeb1702283eda8c78a59cee54fde611e1e273eb422af6b3beeb2ca5b1`. The affected full-size frame passed visual inspection. Public replacement upload is still pending because the ChatGPT Chrome Extension currently rejects local-file injection until the user enables “Allow access to file URLs”; metadata intentionally leaves the YouTube URL unset, so the checker remains red and G09 cannot advance prematurely.
- 2026-07-18: C02 remains `0`, but actual-client discovery/use now has a fail-closed evidence path. A sanitized capture from genuine Codex Desktop session `019f6485-a254-7b30-8da7-f26d254bfcb8` proves client-injected Slidewright registration, explicit selection, installed `SKILL.md` read, and installed/public normalized hash `b91dd98ab729edd3ccb0012cede43004cf66d112e4d5c8e500a5909f6ddc4f7f` at public commit `4bca20216ccdaaed94d43552dc4248652bd4325f`; proof hash `208a734791a6d41618136a57f674830b60a956eaea6deb094bbf64bed87678bd`. The verifier rejects caller labels, self-report-only use, missing skill reads, and invented VS Code originators. Desktop still lacks a predeclared user nonce and therefore has `surfaceComplete=false`; a genuine VS Code extension nonce task against the same public release is also absent. Focused C02/installation tests pass 22/22 without advancing the binary goal.
- 2026-07-18: C13 remains `0`, but the external study is now reproducible and cannot be replaced by agent review. Scorecard `a41166ce5b9eafc78906561ab3a0c60dc602371400bdacc65045d5176ccdae24` deterministically selects 21 byte-unique, perceptually distinct designs from 39 licensed C10 slide lineages across four families, with minimum pairwise 64-bit dHash distance 4 and 40 hash-bound artifacts. The blinded packet hash is `6e32c24ab10d7c52ad52dfc059aba759520aa1a216d963d0b516a15fb576d24a`; its physically separate administrator key hash is `669c3bd8b281adf814abe86e451cde9fc7b381d24a9f705e6c3d641cab60fa97`. Preparation and independent verification pass, focused tests pass 5/5, and the completion command correctly fails with exactly one independent blind human expert, five independent target users, and user coverage of all 21 designs missing. Agents, bots, and implementation-team members are hard-rejected.
- 2026-07-18: C19 remains `0`, but PowerPoint Windows now has real suite evidence on clean commit `6907c48c549be4b6251d07192a1aa8c00bd7b58e`. PowerPoint 16.0 build 20131 performed a native COM edit, save, reopen, and four 1600x900 renders with 45 native text objects, one mixed-emphasis object, two charts, one table, two groups, two connectors, zero full-slide rasters, 15 verified artifact bodies, preserved reading-order names, clean owned-process exit, and 8/8 rejected destructive controls. Source deck hash is `bf1246049daf5973d7c11acd70a5c16d047ba055689d807393fbea942a167af5`, result deck hash `98a9ca847cf3e0da6f2d8e29d0b6f46933d29965152eb751bea48e2f21ec1705`, suite evidence hash `2bf0106d5d20a56991f20e3ffbc54c0eb14e954261f506fd6a193fa6610b7bde`, and independent validation hash `a907b820a82ec235c96667e0ea82f26d0f53b1625f1e1541891c075c9fe58ac2`. All four slides passed individual full-size review. PowerPoint macOS, Google Slides, Keynote, LibreOffice, and Canva remain unproven, so the six-suite importer still rejects completion.
- 2026-07-18: Final release qualification on clean commit `6b83df409dad12d2cabf26922e0796c9ca4b4ec2` correctly stopped after 1,322.4 seconds when the C08 producer received a transient Windows `EPERM` while atomically renaming a newly written hash-addressed run. The destination did not exist, but the publisher incorrectly treated `EPERM` as proof of an existing immutable run and attempted to compare the missing directory. The publisher now distinguishes an existing destination from a transient OneDrive/antivirus filter lock, retries only `EPERM` while the destination is absent with bounded backoff, and still byte-compares an existing run before accepting replay. A deterministic control simulates two transient locks followed by success and proves that an existing destination is never retried; the focused C08 suite passes 26/26. No binary status changes on this repair; a new clean `repair-free` run and independent verification remain mandatory.
- 2026-07-18: The next integrated release run correctly stopped when the feedback-contract PowerPoint worker received transient COM startup error `0x80080005` and left a newly created hidden `/AUTOMATION` process. Exact PID/start-time, sole-process, hidden-window, empty-presentation, and automation-command checks allowed safe cleanup without touching a user presentation. The worker now retries COM initialization with bounded backoff, captures only a unique post-baseline hidden automation process, and uses exact-identity `WM_QUIT` cleanup after COM release; its prior `Stop-Process -Force` fallback is removed. Static controls prove the retry, baseline delta, hidden-window check, exact start identity, `/AUTOMATION` requirement, and absence of `Stop-Process`, `taskkill`, or `.Kill()`. The live producer then passed in 193.8 seconds at scorecard `3d4686a79e1ff261af4a11393e03010b5a2e61ed17ad9c907b0e037e4beae7fe`: 34 slides, 110 native text shapes, zero empty paragraphs, exact state preserved through PowerPoint SaveAs/reopen, and two inherited-template slides pixel-identical before and after sanitization/roundtrip. No binary status changes on this safety repair; a new clean `repair-free` run and independent verification remain mandatory.
